# Managed by tooling, not by us.
pnpm-lock.yaml
node_modules
.svelte-kit
build
.vercel
.data
coverage
test-results
playwright-report

# Committed migrations are drizzle-kit's output — reformatting them would make
# the checksums drift from what the migrator recorded.
drizzle
